# Template only - do not commit real values.
#   kubectl create secret generic lodgezilla-secrets \
#     --from-literal=mongo-uri='mongodb+srv://USER:PASSWORD@HOST/?retryWrites=true&w=majority' \
#     --from-literal=jwt-secret-key="$(openssl rand -hex 32)"
apiVersion: v1
kind: Secret
metadata:
  name: lodgezilla-secrets
type: Opaque
stringData:
  mongo-uri: "mongodb://localhost:27017"
  jwt-secret-key: "change-me"
